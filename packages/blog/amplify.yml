version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd packages/blog
        - rm -rf .nuxt .output node_modules yarn.lock
        - yarn cache clean
        - yarn install
    build:
      commands:
        - cd packages/blog
        - yarn generate
        - node scripts/generate-amplify-rewrites.mjs
  artifacts:
    baseDirectory: packages/blog/.amplify-hosting/static
    files:
      - "**/*"
  cache:
    paths:
      - packages/blog/node_modules/**/*
